# OS
.DS_Store
Thumbs.db

# Dependencies
node_modules/
.npm
.pnpm-debug.log*

# Quasar build outputs
.quasar
dist/
release/
# Quasar compiles quasar.config.cjs to a timestamped temp file and normally deletes it;
# an interrupted build leaves it behind as permanent untracked noise.
quasar.config.*.temporary.compiled.*.cjs

# Cordova
/src-cordova/node_modules
/src-cordova/platforms
/src-cordova/plugins
/src-cordova/www

# Capacitor
/src-capacitor/node_modules
/src-capacitor/www
/src-capacitor/android/
/src-capacitor/ios/

# BEX
/src-bex/www

# Electron
/src-electron/dist

# PWA — scaffold output, not source
#
# `quasar build -m pwa` re-runs the PWA mode installation whenever these are absent, copying
# @quasar/app-vite/templates/pwa/common verbatim. Verified 2026-08-17 by removing both and
# rebuilding: the build succeeded and recreated them byte-identical to the template.
#
# So deleting them is futile and tracking them is wrong — they carry nothing project-specific,
# they duplicate deps this package.json already declares at newer ranges, and they would drift
# against the template on every Quasar upgrade. The real PWA sources next to them
# (manifest.json, register-sw.ts, sw/) stay tracked.
/src-pwa/package.json
/src-pwa/package-lock.json

# IDE
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.history/
*.sublime-workspace

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Testing
playwright-report/
test-results/
coverage/
coverage-free/
testing/e2e-docker/output/
testing/e2e-engram-docker/output/

# Audit & test reports (auto-generated by scripts/)
reports/
# Exception: LLGD coverage baseline is a point-in-time archival artifact
# (v1.0.2 → v1.0.4 before/after comparison; spec tracked internally).
# Re-runs of the script overwrite the tracked file; commits capture each
# snapshot across release cycles. Directory itself must be un-ignored so
# gitignore considers files inside it.
!reports/
reports/*
!reports/llgd-coverage-baseline.json
!reports/llgd-coverage-baseline.md

# Manual test baseline (user-local state)
testing/.last-verified-sha
scripts/.fresh-install-count

# Playwright auth state (generated by E2E global-setup)
.auth/

# Stryker mutation testing
.stryker-tmp/

# Temporary files
tmp/

# Quasar SCSS compiled output (build artifacts — source is *.scss)
src/css/*.css
src/css/*.css.map

# Live E2E (nix-fresh-test.sh runtime state)
backend/data-live-e2e/
testing/live-e2e-output/
.live-e2e-backend.pid
.live-e2e-backend.log

# Data (user-generated)
data/
!backend/data/
backend/data/*
!backend/data/distro-catalog.json
!scripts/data/

# SBOM (build artifacts from CycloneDX)
sbom*.json

# Environment
.env
.env.local
.env.*.local

# A security questionnaire answer sheet used to be ignored here as a "personal working note".
# Ignoring a file inside a synced tree keeps it out of the public mirror, but it also
# keeps it out of git entirely — no history, and no backup, since a git bundle cannot
# capture what git is told to ignore. It now lives at ../docs/security/ in the private
# tree: versioned, backed up, and still never synced (the sync source is code/).
