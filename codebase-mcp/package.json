{
  "name": "weaver-codebase-mcp",
  "version": "0.1.0",
  "description": "Codebase MCP server — exposes Weaver source structure to Claude Code agents",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "npx tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
